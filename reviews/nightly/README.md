# Nightly review entries

One file per day, named `YYYY-MM-DD.md`. Written by Claude when Marko types
"nightly" in a Claude Code session on this repo — see the protocol in the
root `CLAUDE.md`.

This directory is deliberately outside `/brain`: `scripts/sync-brain.ts`
only scans `/brain`, so nothing here is ever indexed into the deployed
app's Docs section. These are personal working notes, not company
knowledge.
